from typing import List

class GraphData:
    def __init__(self, x: List, y: List, title: str, x_label: str, y_label: str):
        self.x = x
        self.y = y
        self.title = title
        self.x_label = x_label
        self.y_label = y_label
